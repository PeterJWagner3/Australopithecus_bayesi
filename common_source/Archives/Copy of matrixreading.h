#ifdef matrixreading	#include <stdlib.h>	#include <stdio.h>	#include <time.h>	#include <math.h>	#include <string.h>	int *numberstates(long **omat, int notu, int nchars, int UNKNOWN, int GAP);	int *autapomorphies(long **omat, int notu, int nchars, int UNKNOWN, int GAP);#else	extern int *numberstates(long **omat, int notu, int nchars, int UNKNOWN, int GAP);	extern int *autapomorphies(long **omat, int notu, int nchars, int UNKNOWN, int GAP);#endif