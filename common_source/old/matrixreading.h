#ifdef matrixreading	#include <stdlib.h>	#include <stdio.h>	#include <time.h>	#include <math.h>	#include <string.h>	int *numberstates(long **omat, int notu, int nchars, int UNKNOWN, int GAP);	int no_states(long **omat, int CH, int notu, int UNKNOWN, int GAP);	int *autapomorphies(long **omat, int notu, int nchars, int UNKNOWN, int GAP);	int autapo_char(long **omat, int notu, int CH, int UNKNOWN, int GAP, int OutCond);	int *unknownstates(long **omat, int notu, int nchars, int UNKNOWN, int GAP);#else	extern int *numberstates(long **omat, int notu, int nchars, int UNKNOWN, int GAP);	extern int no_states(long **omat, int CH, int notu, int UNKNOWN, int GAP);	extern int *autapomorphies(long **omat, int notu, int nchars, int UNKNOWN, int GAP);	extern int autapo_char(long **omat, int notu, int CH, int UNKNOWN, int GAP, int OutCond);	extern int *unknownstates(long **omat, int notu, int nchars, int UNKNOWN, int GAP);#endif